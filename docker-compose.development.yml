version: "3.8"

services:
    web:
        build:
            context: .
            dockerfile: ./builder/Dockerfile.development
        command: npm run dev
        volumes:
            - ./:/app
        environment:
            - APP_NAME
